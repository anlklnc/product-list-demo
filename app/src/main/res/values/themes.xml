<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ProductListDemo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/light_primary</item>
        <item name="android:windowBackground">@color/light_primary</item>
    </style>

    <style name="Theme.ProductListDemo" parent="Base.Theme.ProductListDemo" />
</resources>